# Railway 배포 설정 (선택)
# Root Directory = server 로 두고 빌드하면 server/Dockerfile 이 자동 사용됩니다.
# install-page는 GitHub Pages로 서빙하므로 Dockerfile에서 복사하지 않음.
# (repo 루트에서 Dockerfile 쓰려면 아래 주석 해제 후 Root Directory 비우기)
# [build]
# dockerfilePath = "server/Dockerfile"
