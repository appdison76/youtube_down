# 노트북 서버 설정 가이드 (ngrok 유료 버전)

## 🎯 최종 구성

```
오래된 그램 노트북
  ↓
Node.js 서버 실행
  ↓
ngrok 유료 ($8/월, 고정 URL)
  ↓
외부 접속 가능 ✅
Residential IP 사용 (차단 우회) ✅
```

---

## 📋 설정 단계

### 1단계: 노트북 전원 설정

#### Windows 전원 설정
1. **설정 → 시스템 → 전원 및 절전**
2. **추가 전원 설정** 클릭
3. **전원 옵션 선택** → **"전원 절약"** 또는 **"절전"** 선택
4. **"덮개를 닫을 때 수행할 작업 선택"**
   - **덮개를 닫을 때**: **"아무 작업도 안 함"** ✅
5. **"절전 모드"**: **"안 함"** ✅

#### 고급 전원 설정 (성능 최하)
1. **"전원 옵션 변경"** → **"고급 전원 설정 변경"**
2. **프로세서 전원 관리**
   - **최소 프로세서 상태**: 5%
   - **최대 프로세서 상태**: 50% (서버용으로 충분)
3. **그래픽 전원 관리**
   - **전원 절약 모드** 선택

---

### 2단계: 서버 설치 및 실행

#### 1. Node.js 설치 확인
```bash
node --version
npm --version
```

#### 2. 서버 디렉토리로 이동
```bash
cd c:\projects\youtube_down\server
```

#### 3. 의존성 설치
```bash
npm install
```

#### 4. 서버 실행
```bash
npm start
```

서버가 `http://localhost:3000`에서 실행되는지 확인

---

### 3단계: ngrok 유료 버전 설정

#### 1. ngrok 가입
- https://ngrok.com 접속
- 가입 후 유료 플랜 선택 ($8/월)
- 고정 도메인 설정 (예: `myapp.ngrok.io`)

#### 2. ngrok 설치
```bash
# Windows
# Chocolatey 사용
choco install ngrok

# 또는 직접 다운로드
# https://ngrok.com/download
```

#### 3. ngrok 인증
```bash
ngrok config add-authtoken YOUR_AUTH_TOKEN
```

#### 4. ngrok 실행 (고정 도메인)
```bash
ngrok http 3000 --domain=myapp.ngrok.io
```

출력 예시:
```
Forwarding  https://myapp.ngrok.io -> http://localhost:3000
```

---

### 4단계: config.json 업데이트

#### 1. config.json 파일 수정
```json
{
  "apiBaseUrl": "https://myapp.ngrok.io",
  "version": "1.1.0",
  "updatedAt": "2026-01-26T00:00:00Z",
  "description": "노트북 서버 + ngrok 유료 버전 사용"
}
```

#### 2. GitHub에 푸시
```bash
cd c:\projects\youtube_down
git add install-page/config.json
git commit -m "Update server URL to ngrok"
git push
```

---

### 5단계: 자동 시작 설정 (선택사항)

#### Windows 작업 스케줄러로 자동 시작

1. **작업 스케줄러** 열기
2. **기본 작업 만들기**
3. **트리거**: "컴퓨터 시작 시"
4. **동작**: "프로그램 시작"
   - 프로그램: `node`
   - 인수: `c:\projects\youtube_down\server\server.js`
   - 시작 위치: `c:\projects\youtube_down\server`

#### ngrok 자동 시작 (배치 파일)

`start-server.bat` 파일 생성:
```batch
@echo off
cd c:\projects\youtube_down\server
start "Server" node server.js
timeout /t 5
start "Ngrok" ngrok http 3000 --domain=myapp.ngrok.io
```

작업 스케줄러에서 이 배치 파일을 자동 시작으로 설정

---

## ✅ 확인 사항

### 1. 서버 실행 확인
```bash
curl http://localhost:3000/health
```

### 2. ngrok 연결 확인
```bash
curl https://myapp.ngrok.io/health
```

### 3. 앱에서 접속 확인
- 앱 실행
- 서버 연결 테스트
- YouTube 다운로드 테스트

---

## 🔧 문제 해결

### 서버가 시작되지 않음
- Node.js 설치 확인
- 포트 3000이 사용 중인지 확인
- 방화벽 설정 확인

### ngrok 연결 실패
- 인터넷 연결 확인
- ngrok 인증 토큰 확인
- 고정 도메인 설정 확인

### 앱에서 접속 불가
- config.json이 올바르게 업데이트되었는지 확인
- GitHub Pages에서 config.json 접근 가능한지 확인
- ngrok URL이 올바른지 확인

---

## 💰 비용 요약

- **노트북**: 무료 (이미 보유)
- **ngrok 유료**: $8/월
- **총 비용**: $8/월

vs

- **Railway**: $5/월
- **프록시 서비스**: $65-300/월
- **총 비용**: $70-305/월

**절약**: $62-297/월

---

## 🎯 장점

- ✅ 비용 절감 ($8/월)
- ✅ Residential IP 사용 (차단 우회)
- ✅ 고정 URL (ngrok 유료)
- ✅ 오래된 노트북 활용

## ⚠️ 단점

- ⚠️ 노트북이 항상 켜져 있어야 함
- ⚠️ 전력 소모
- ⚠️ 발열 관리 필요

---

## 📝 체크리스트

- [ ] 노트북 전원 설정 (절전 모드 방지)
- [ ] 성능 최하 설정 (전원 절약 모드)
- [ ] 서버 설치 및 실행 확인
- [ ] ngrok 유료 버전 가입 및 설정
- [ ] config.json 업데이트
- [ ] GitHub에 푸시
- [ ] 앱에서 접속 테스트
- [ ] 자동 시작 설정 (선택)

---

## 🚀 빠른 시작

1. **노트북 전원 설정** (절전 모드 방지)
2. **서버 실행**: `cd server && npm start`
3. **ngrok 실행**: `ngrok http 3000 --domain=myapp.ngrok.io`
4. **config.json 업데이트**: `https://myapp.ngrok.io`
5. **GitHub에 푸시**
6. **앱에서 테스트**

완료!
