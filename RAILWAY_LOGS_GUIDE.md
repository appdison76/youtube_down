# Railway 로그 확인 가이드

## 📋 Railway 로그 보는 방법

### 1. Railway 웹 대시보드에서 확인

1. **Railway 웹사이트 접속**
   - https://railway.app 접속
   - 로그인

2. **프로젝트 선택**
   - 대시보드에서 `youtubedown-production` 프로젝트 클릭

3. **서비스(Service) 선택**
   - 프로젝트 내에서 서버 서비스 클릭

4. **로그 확인**
   - 좌측 메뉴에서 **"Logs"** 탭 클릭
   - 또는 서비스 페이지에서 **"View Logs"** 버튼 클릭
   - 실시간 로그를 확인할 수 있습니다

### 2. Railway CLI로 확인 (선택사항)

```bash
# Railway CLI 설치 (없는 경우)
npm i -g @railway/cli

# 로그인
railway login

# 프로젝트 연결
railway link

# 로그 보기
railway logs

# 실시간 로그 스트리밍
railway logs --follow
```

### 3. 로그에서 확인할 내용

**정상 작동 시:**
- 서버 시작 메시지: `Server is running on port ...`
- API 요청 로그: `GET /api/video-info`, `GET /api/download/audio` 등
- yt-dlp 다운로드 진행 상황

**오류 발생 시:**
- 연결 오류: `ECONNREFUSED`, `ETIMEDOUT` 등
- Python/yt-dlp 오류: `yt-dlp` 관련 에러 메시지
- 포트 오류: `EADDRINUSE` 등

### 4. 로그 필터링

Railway 로그 화면에서:
- 특정 키워드 검색 가능
- 로그 레벨별 필터링 (ERROR, WARN, INFO)
- 시간 범위 선택 가능

## 🔍 문제 해결 팁

1. **서버가 응답하지 않는 경우:**
   - 로그에서 서버 시작 메시지 확인
   - 포트 설정 확인 (`process.env.PORT` 사용 확인)

2. **yt-dlp 오류:**
   - Python 버전 확인
   - yt-dlp 설치 여부 확인
   - 로그에서 상세 오류 메시지 확인

3. **연결 타임아웃:**
   - Railway 서버가 실행 중인지 확인
   - 방화벽/네트워크 설정 확인

## 📝 현재 서버 정보

- **Railway URL**: `https://youtubedown-production.up.railway.app`
- **서버 위치**: Railway 클라우드
- **로그 위치**: Railway 대시보드 > 프로젝트 > 서비스 > Logs

