# Nixpacks configuration for Railway
# Python + Node.js 환경 설정

[phases.setup]
nixPkgs = ["python311", "pip", "nodejs-18_x"]

[phases.install]
cmds = [
  "pip install yt-dlp",
  "npm install"
]

[start]
cmd = "npm start"


